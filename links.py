from switch_addon import switch, case
from json_s_l import save,load

def init():
    global all_links


def execute_link(id):
    global all_links
    link = all_links[id]
    while(switch(link_type)):
        if case(0):

        if case(0):


